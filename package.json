{
  "name": "janua",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "concurrently -n core,api,widget,admin -c yellow,blue,magenta,cyan \"pnpm --filter @janua/core dev\" \"pnpm --filter @janua/api dev\" \"pnpm --filter @janua/widget dev\" \"pnpm --filter @janua/admin dev\"",
    "db:migrate": "pnpm --filter @janua/core db:migrate",
    "test": "pnpm -r test",
    "check": "pnpm build && pnpm -r --if-present typecheck && pnpm test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
